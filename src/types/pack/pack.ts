export type Pack = {
    monkeyCoinPackId: string;
    imgUrl: string;
    price: number;
    point: number;
};

export type CreatePackPayload = {
    imgUrl: string;
    price: number;
    point: number;
};
